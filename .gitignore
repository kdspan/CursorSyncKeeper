# Build output (generated by CMake / Ninja)
build/

# Published installer artifact (distributed via GitHub Releases, not the repo)
dist/

# Build / packaging logs
*.log

# CodeBuddy project data
.codebuddy/

# Editor / IDE (personal workspace config)
.vs/
*.user
.vscode/settings.json
*.code-workspace

# OS
Thumbs.db
Desktop.ini
# Stray local files (never commit)  
build_log.txt  
commit_msg.txt  
